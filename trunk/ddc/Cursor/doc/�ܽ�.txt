2008年10月2日
1.窗口拆分问题
  通过看文档，动态窗口拆分的窗格必须是相同的类;静态拆分后只能修改窗格的大小，不能修改窗格的数量和顺序。静态拆分的窗格必须在父框架的OnCreateClient函数返回前建立完成，否则框架不能正确显示该窗口。
2.VC对齐
  通过测试发现VC的对齐(比如左右对齐等)以最后点击的那个控件为基准，其它的控件向它对齐。
3. 灰色背景 192,192,192
红色头 128，0， 0
深蓝色 0，0，128

4.在程序中获取其它程序的图标  SHGetFileInfo()
5.与windows窗口有关的函数搜索“Window Functions”