2008.12.28
1.可以保存fft图像和原始数据(文本格式)。
2.使用新的matlab算法

2008.12.29
1.增加一个采集和计算结果的线程，用户界面的处理和底层接口处理分离，使用户界面能够及时响应。
2.调用matlab计算前，先把输出参数清零，防止某些数据由于计算不出结果而保持上一个通道的结果，造成4个通道的计算结果相同。

2009.1.3
1.动态库libadc.dll修改为与钱的算法一样，输入的是正负的数据。
2.动态库libfft.dll修改窗函数为hanning，查找最大值从第一个点开始，不再忽略直流分量(因为输入信号为正负，所以直流分量已经去掉)
3.原始数据视图和fft视图增加一些参数的显示

2009.1.4
1.DataDisp的最大值和最小值在一个周期80个点里面查询
2.FFTDisp显示波形时增加的偏移带来的问题进行修正
3.批量测试的正确计数和错误计数添加
4.通道串音出现负数的问题修正

2009.1.14
1.首先完成090110文件规定的修改。
2.IQ采集数据的流程完成，需要测试
3.DataDisp中最大值和最小值的查找范围扩大到当前视图，不仅仅局限在一个周期内，解决Snap1.gif暴露的问题。

2009.1.15
1.以目前的测试模式，修改保存和打开的代码，只保存当前显示的数据。
2.修改算法测试的参数对话框，由无模式改为模式
3.在算法测试模式下，视图显示为8路原始数据，频谱的显示还没有设计。

2009.1.16
1.频谱显示的最低点由-120dB修改为-160dB
2.算法测试保存的文件由二进制文件修改为文本文件

2008.1.17
1.把matlab的画图功能作为可选项，添加到面板上。

2008.2.8
1.fft视图显示的最小值为-160db，实际计算使用的最小值为-120dB，把计算使用的最小值修改为-160dB

2008.2.26
1.增加光纤状态的检测

2009年3月7日
1.完成4个通道同时显示，支持工具栏的缩放，但是右键菜单有时候可以，有时候不行??

2009年3月18日
1.算法测试模式增加了几个参数的显示，参数的计算方法使用52所提供的，注意与ADC测试模式的区别是ADC测试使用的matlab函数输入向量为行向量，而算法测试的matlab函数输入向量为列向量。

2009年3月20日
1.原来的程序fft视图只能更新前面的数据，后面的数据不更新，问题定位在DrawCurve()里面的
		// 先进行修正
		for ( i = 0; i < m_dwDot; i++ )
		{
			//m_FFTData[i] += dErr;
			m_daDispData[i] = m_FFTData[i] + dErr;
		}
显示的数据只更新前面的m_dwDot个点，需要把m_dwDot修改为MAX_DEPTH，所有数据都要更新，在视图显示的只是一部分。

2009年3月27日
1.增加了标记，没有显示对应的值。
2.fft的坐标显示为整数。
3.缩放时大于MAX_DEPTH / 2的点不显示，原来显示的一条横线。

2009年7月19日
1.初始显示的视图由AD视图修改为算法测试视图;
2.在r值修改后，fft视图的横坐标重新按照新值绘制;

2009年8月6日
1.在fft视图的顶部添加5个最大值的显示

2009年8月10日
1.独立配置的方式进行修改，每个通道单独送地址，参数，命令；回读方式也进行相应修改，每个通道单独进行

2009年8月14日
1.4个通道的r值单独设置

2009年8月19日
1.在两个线程之间增加同步机制，保护4个通道的缓冲区

2009年8月26日
1.在fft视图上增加鼠标当前坐标值的显示

2009年9月20日
1.软件名称和图标进行修改
2.上板电源上电、断电功能、图标

2009年9月24日
1.批量测试的流程基本理顺，具体测试内容带补充

2009年9月25日
1.原来的程序在进行ADC测试时有bug，一是发送的命令参数为0x11, 应该是0x01，待确认;二是测试模式没有设置m_nTestModeSel，如果开始就使用adc测试没问题，在进行模式切换后会有问题。

2009年10月9日
1.自动载入参数文件

2009年10月13日
1.测试每一项都连续进行，直到用户点击测试正确或者错误
2."重新测试"测试当前项
3."放弃测试"回到初始